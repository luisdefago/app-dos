import { View, Text } from 'react-native';
import { styles } from './styles';

const Orders = () => {
    return (
        <View>
            <Text>Orders</Text>
        </View>
    );
};

export default Orders;
